<link rel = "stylesheet" href="/css/style.css">

 <h1>Feuille de Match</h1>

  <form action="/newCompo" method= "POST">

<!-- Choix Date -->
      Date : 
      <input type="date" id="date" value="" min="2021-01-01" max="2022-01-01" required>

<!-- Choix Adversaire -->   
      Adversaire :
      <select name="adversaire" id="">
         <% for (let i = 0 ; i < adversaires.length ; i++) { %> 
            <option value="<%=adversaires[i].id_adversaire %>"> <%= adversaires[i].nom %> </option>
         <% } %>
      </select>   

<!-- Joueurs -->

   1/ <select name="joueur1" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>

   2/ <select name="joueur2" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select> 

   3/ <select name="joueur3" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>
   
   <br>
   <br>
   4/ <select name="joueur4" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>
   5/ <select name="joueur5" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>
   <br>
   <br>
   6/ <select name="joueur6" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>
   8/ <select name="joueur7" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select> 
   
   7/ <select name="joueur8" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>     
   <br>
   <br>
   9/ <select name="joueur9" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>  
   <br> 
   <br>

   10/ <select name="joueur10" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>  
   
   <br>
   <br>
   12/ <select name="joueur12" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select>  
   13/ <select name="joueur13" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select> 

   <br>
   <br>
   11/ <select name="joueur11" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select> 
   15/ <select name="joueur15" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select> 

   14/ <select name="joueur14" id="">
      <% for (let i = 0 ; i < joueurs.length ; i++) { %> 
         <option value="<%=joueurs[i].id %>" >
            <%= joueurs[i].nom %> <%= joueurs[i].prenom %>
         </option>
      <% } %>
   </select> 
   <br>
   <br>
   <br>
   <br>
   <br>
     <a href="/nouvelle_composition"> <input type="button" name="cancel" value="Retour"/> </a>
     <input type="submit" name="save" value="Sauvegarder la Composition" />

  </form>   
